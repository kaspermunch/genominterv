
from setuptools import setup

setup(name='GenomicIntervals',
      version='1.0',
      description='Utilities for working with intervals in separate chromosomes.',
      url='https://github.com/kaspermunch/GenomicIntervals',
      author='Kasper Munch',
      packages=['GenomicIntervals'],
      zip_safe=False)

